import { getPublicBusinesses, getProductsForBusiness } from "@/lib/data/queries";
export default async function AdminPage(){const businesses=await getPublicBusinesses(); const counts=await Promise.all(businesses.map(async(b)=>({id:b.id,count:(await getProductsForBusiness(b.id)).length}))); return <main className="mx-auto max-w-6xl px-4 py-10"><h1 className="text-4xl font-black">Panel administrador</h1><p className="mt-2 text-slate-600">Vista global para activar/desactivar comercios y revisar productos.</p><div className="card mt-6 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th className="py-3">Comercio</th><th>Zona</th><th>Estado</th><th>Activo</th><th>Productos activos</th></tr></thead><tbody>{businesses.map((b)=><tr className="border-b last:border-0" key={b.id}><td className="py-4 font-bold">{b.name}</td><td>{b.zone}</td><td>{b.is_open?"Abierto":"Cerrado"}</td><td>{b.is_active?"Sí":"No"}</td><td>{counts.find((c)=>c.id===b.id)?.count ?? 0}</td></tr>)}</tbody></table></div></main>}
